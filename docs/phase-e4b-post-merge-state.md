# UI E4b post-merge state

After this PR merges:

- UI has explicit README projection receipt rows.
- UI projection evidence points to those receipt rows.
- UI package receipt includes README projection evidence.
- UI governance validation uploads the receipt file as package evidence.
- Preview output remains non-final and non-authority.
