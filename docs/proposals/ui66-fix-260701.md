# UI package evidence checks

## Purpose

Replace stale PR #66 on the current `proposals` base and finish the UI-owned package evidence responsibility.

## Change

- Add UI package response output for projection evidence, receipts, artifact boundary, and governance check adoption.
- Add projection evidence, artifact boundary proof, receipt, and residual files.
- Add `gov-package-validation.yml` so UI CI runs the reusable governance package checker.
- Keep generated UI artifacts as evidence only, not source authority.
- Keep the existing strict CI workflow contract checks and extend them for the governance validation workflow.

## Proof

- `tests/check-ui-package-evidence.mjs` validates response rows, evidence refs, receipts, and closed residuals.
- `tests/check-ci-workflows.mjs` validates all expected workflow intent rows and workflow contents.
- CI must pass Governance package validation, Nix Flake Check, A2UI adapter artifacts, and README artifact exporter on the PR head.

## Boundary

This PR does not claim that UI is meaning authority. It only proves that UI returns package-level evidence for its owned projection and artifact boundary obligations.
