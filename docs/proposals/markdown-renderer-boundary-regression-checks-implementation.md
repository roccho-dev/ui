# Markdown renderer boundary regression implementation

This PR adds `tests/check-markdown-renderer-boundary-regressions.mjs`.

It covers ADR row reads, governance policy validation, README artifact HTML output, and artifact upload lifecycle ownership.
