# UI E4a boundary

This PR only closes `ui#96` governance input and packet producer adoption.

It does not close:

- `ui#97` package evidence expansion;
- `ui#98` local cutover;
- governance final gate cutover;
- org-wide active admission;
- ADRS meaning authority.
