# Verdict: use `main` as base

Use `main` as the base branch for this proposal.

`main` contains the current accepted UI model changes. A proposal branch for CI should start from it so the built HTML is checked against the code that will actually receive the merge.
