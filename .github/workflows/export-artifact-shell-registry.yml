name: refresh artifact-shell registry

on:
  push:
    branches:
      - codex/ui-191-version-fixed-invocation

permissions:
  contents: write

jobs:
  refresh:
    if: ${{ !contains(github.event.head_commit.message, '[registry-generated]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Regenerate and commit exact registry bytes
        run: |
          node apps/artifact-shell/scripts/build-registry.mjs
          if git diff --quiet -- apps/artifact-shell/generated/capability-registry.mjs; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add apps/artifact-shell/generated/capability-registry.mjs
          git commit -m "chore: refresh artifact-shell registry [registry-generated]"
          git push origin HEAD:${GITHUB_REF_NAME}
